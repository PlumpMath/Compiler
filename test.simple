function array(ax: int) : bool
{
  var a: intarray[12];
  var b: intarray[12];
  var x: int;
  var z: int;

  a[2+3] = b[0];
    
  a[0] = x*7 + b[1];


  return false;
}

function Main() : int
{
    var x: int;
    var k,j :int ;
    var t: bool;
    t=true;
    x = 5;
    
    t = t||true;

    if (x < 3) {
        x = 7;
    }
    if (x <3) {
        x = 9;
    }
    else {
        if (true&&t) {
            x = 12;
        }
    }
   
    return 0;
}

function f(ax: int) : int
{
    var l: bool;
    var d: int;

    function nested() : bool
    {
        var  i, a : int;
    return i-a == 12;
    }

    return -(---d+d*d/d);
}

function main(a: int) : bool
{
    var i : int;

    function  tain(integer: int) : bool
    {
        return false || true;
    }
    return (2+2*4) < 3;
}

function main1(ax: int) : bool
{
    var i: int;
    return (2+2*4) < 3;
}


